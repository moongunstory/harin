{
  "rules": {
    ".read": false,
    ".write": false,
    "GlobalStats": {
      ".read": true,
      "$axis": {
        "$type": {
          ".write": "auth != null && newData.isNumber() && newData.val() >= data.val() && newData.val() <= data.val() + 50"
        }
      }
    },
    "Users": {
      "$uid": {
        ".read": true,
        ".write": "auth != null && auth.uid === $uid",
        "nickname": {
          ".validate": "newData.isString() && newData.val().matches(/^[0-9A-Za-z._\\-가-힣 ]{2,20}$/)"
        },
        "mbti_type": {
          ".validate": "newData.isString() && newData.val().matches(/^(|ENFJ|ENFP|ENTJ|ENTP|ESFJ|ESFP|ESTJ|ESTP|INFJ|INFP|INTJ|INTP|ISFJ|ISFP|ISTJ|ISTP)$/)"
        },
        "bio": {
          ".validate": "newData.isString() && newData.val().length <= 40"
        },
        "equipped_title": {
          ".validate": "newData.isString() && newData.val().length <= 40"
        },
        "total_clicks": {
          ".validate": "newData.isNumber() && newData.val() >= 0"
        },
        "clicks": {
          "$type": {
            ".validate": "newData.isNumber() && newData.val() >= 0"
          }
        },
        "$other": {
          ".validate": true
        }
      }
    },
    "GlobalChat": {
      ".read": true,
      "$msgId": {
        ".write": "auth != null && newData.child('userId').val() === auth.uid",
        "userId": {
          ".validate": "newData.isString() && newData.val() === auth.uid"
        },
        "nickname": {
          ".validate": "newData.isString() && newData.val().matches(/^[0-9A-Za-z._\\-가-힣 ]{2,20}$/)"
        },
        "mbtiType": {
          ".validate": "newData.isString() && newData.val().matches(/^(|ENFJ|ENFP|ENTJ|ENTP|ESFJ|ESFP|ESTJ|ESTP|INFJ|INFP|INTJ|INTP|ISFJ|ISFP|ISTJ|ISTP)$/)"
        },
        "text": {
          ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 60"
        },
        "timestamp": {
          ".validate": "newData.isNumber() || newData.val() == now"
        },
        "$other": {
          ".validate": false
        }
      }
    },
    "MatchQueue": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['nickname','timestamp']) || !newData.exists()"
      }
    },
    "UserStatus": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "PrivateRooms": {
      "$roomId": {
        ".read": "auth != null && data.child('info/participants/' + auth.uid).exists()",
        "info": {
          ".write": "auth != null && (data.child('participants/' + auth.uid).exists() || (!data.exists() && newData.child('participants/' + auth.uid).exists()))"
        },
        "messages": {
          "$msgId": {
            ".write": "auth != null && root.child('PrivateRooms/' + $roomId + '/info/participants/' + auth.uid).exists()",
            "userId": {
              ".validate": "newData.isString() && newData.val() === auth.uid"
            },
            "nickname": {
              ".validate": "newData.isString() && newData.val().matches(/^[0-9A-Za-z._\\-가-힣 ]{2,20}$/)"
            },
            "text": {
              ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 60"
            },
            "timestamp": {
              ".validate": "newData.isNumber() || newData.val() == now"
            },
            "$other": {
              ".validate": false
            }
          }
        }
      }
    },
    "makgoraStats": {
      "$uid": {
        ".read": true,
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "makgoraHistory": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "HeadToHead": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "MakgoraInvites": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null"
      }
    },
    "MakgoraQueue": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "events": {
      "superchat": {
        ".read": true,
        ".write": false
      }
    },
    "donations": {
      "auth": {
        "$txid": {
          ".read": false,
          ".write": false
        }
      }
    }
  }
}
